import React from 'react';
interface LayoutPresenterProps {
    children?: React.ReactNode;
    title?: string;
}
export declare const LayoutPresenter: ({ children, title }: LayoutPresenterProps) => React.JSX.Element;
export {};
