import React from 'react';
import type { UserAvatarSize } from '../UserAvatar';
import type { QAProps } from '../types';
export interface UserProps extends QAProps {
    name?: string;
    description?: string;
    imgUrl?: string;
    size?: UserAvatarSize;
    className?: string;
}
export declare function User({ name, description, imgUrl, size, className, qa }: UserProps): React.JSX.Element;
