import type { PersonaText } from './types';
export declare const extractTextValue: (text?: PersonaText) => string;
export declare const extractTextView: (text?: PersonaText) => import("react").ReactNode;
export declare function getTwoLetters(text: string): string;
