import type { Duration, DurationInput, DurationUnit } from '../typings';
interface Options {
    lang?: string;
}
export declare function createDuration(amount: DurationInput, unit?: DurationUnit, options?: Options): Duration;
export declare function createDuration(amount: DurationInput, options?: Options): Duration;
export {};
