import type { Locale, Parser, PublicSettings, UpdateLocaleConfig } from './types';
declare class Settings implements PublicSettings {
    private loadedLocales;
    private defaultLocale;
    private defaultTimeZone;
    private parser;
    constructor();
    loadLocale(locale: string): Promise<void>;
    getLocale(): string;
    getLocaleData(): Locale;
    setLocale(locale: string): void;
    updateLocale(config: UpdateLocaleConfig): void;
    setDefaultTimeZone(zone: 'system' | (string & {})): void;
    getDefaultTimeZone(): string;
    setRelativeParser(parser: Parser): void;
    getRelativeParser(): Parser;
    private isLocaleLoaded;
}
/**
 * Settings to manage DateTime customization
 */
export declare const settings: Settings;
export {};
