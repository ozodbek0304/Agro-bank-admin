import type { PlotLineConfig } from '../../types';
import type uPlot from 'uplot';
import type Yagr from '../../index';
import { LabelsOptions } from './types';
type StoredPlotLabel = {
    band: PlotLineConfig;
    label: string | null | undefined;
};
/**
 * Sets up hooks for labels rending on plotLines draw
 */
export declare function plotLinesDrawBasedLabels(yagr: Yagr, hooks: uPlot.Hooks.Arrays, options: LabelsOptions): {
    getPlotLinesLabels: () => {
        band: PlotLineConfig;
        label: string | ((band: PlotLineConfig) => string | null | undefined) | undefined;
    }[];
    getCurrentPlotLinesLabels: () => StoredPlotLabel[];
};
export {};
