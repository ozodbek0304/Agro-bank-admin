import type uPlot from 'uplot';
import type Yagr from '../../index';
import { AxisLabel, LabelsOptions } from './types';
export declare function axisDrawBasedLabels(yagr: Yagr, hooks: uPlot.Hooks.Arrays, options: LabelsOptions): {
    getAxisLabels(): {
        [axis: string]: AxisLabel[];
    };
    getCurrentAxisLabels(proximity: {
        x: number;
        y: number;
    }): Record<string, AxisLabel[]>;
};
