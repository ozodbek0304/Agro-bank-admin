import UPlot, { Range } from 'uplot';
import { YagrConfig, Scale } from '../types';
import type Yagr from '../index';
export declare const getScaleRange: (scale: Scale, config: YagrConfig) => Range.MinMax | ((_: UPlot, dMin: number, dMax: number) => Range.MinMax) | undefined;
export declare function offsetScale(dataMin: number, dataMax: number, scaleConfig: Scale): {
    min: number;
    max: number;
};
export declare function niceScale(dataMin: number, dataMax: number, scaleConfig: Scale): {
    min: number;
    max: number;
};
export declare function configureScales(yagr: Yagr, scales: UPlot.Scales, config: YagrConfig): UPlot.Scales;
