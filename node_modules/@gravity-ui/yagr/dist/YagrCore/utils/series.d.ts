import { Series } from 'uplot';
import type Yagr from '..';
import { RawSerieData } from '../types';
export declare function configureSeries(yagr: Yagr, rawSeries: RawSerieData, idx: number): Series;
export declare const overrideSeriesInUpdate: (dest: Series, source: Series) => void;
