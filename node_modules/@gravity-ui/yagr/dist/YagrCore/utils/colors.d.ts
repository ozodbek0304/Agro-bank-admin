import UPlot, { Series } from 'uplot';
import type Yagr from '../index';
/**
 * This class implements:
 *  - color parsing from RGBA, HEX or CSS color name
 *  - linear shade/lighten @see https://github.com/PimpTrizkit/PJs/wiki/12.-Shade,-Blend-and-Convert-a-Web-Color-(pSBC.js)#stackoverflow-archive-begin
 */
export default class ColorParser {
    static parseRgba(rgbaColor: string): number[] | null;
    private context?;
    parse(color?: string): string;
    setContext(context: HTMLElement): void;
    rgba(color: number[]): string;
    toRgba(color: string, fallbackRgba: number[]): number[];
    shade([r, g, b, a]: number[], value: number): string;
}
export declare const getFocusedColor: (y: Yagr, seriesIdx: number) => string;
export declare const getSerieFocusColors: (y: Yagr, field: keyof Series) => (u: UPlot, idx: number) => string;
