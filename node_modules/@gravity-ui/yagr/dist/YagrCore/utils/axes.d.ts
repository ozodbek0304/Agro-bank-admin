import uPlot from 'uplot';
import type { Axis } from 'uplot';
import type Yagr from '../../';
import { YagrConfig, AxisOptions } from '../types';
export declare const getAxisPositioning: (side: AxisOptions['side'], align: Axis['align']) => {
    side: Axis.Side;
    align: Axis.Align | undefined;
};
export declare const getDefaultNumberFormatter: (precision: 'auto' | number, nullValue?: string) => (n: number | null) => string;
export declare const getTimeFormatter: (config: YagrConfig) => (_: unknown, ticks: number[]) => string[];
export declare function getRedrawOptionsForAxesUpdate(axes: YagrConfig['axes']): [series: boolean, axes: boolean];
export declare function updateAxis(yagr: Yagr, uAxis: Axis, axisConfig: AxisOptions): void;
export declare function configureAxes(yagr: Yagr, config: YagrConfig): uPlot.Axis[];
