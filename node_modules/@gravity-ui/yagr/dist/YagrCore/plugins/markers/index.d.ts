import type Yagr from '../../index';
import UPlot, { Plugin } from 'uplot';
import { YagrConfig } from '../../types';
export declare const renderCircle: (u: UPlot, x: number, y: number, r: number, s: number, color: string, strokeColor: string, yScale?: string) => void;
export declare function drawMarkersIfRequired(u: UPlot, i: number, i0: number, i1: number): false | undefined;
export default function YagrMarkersPlugin(yagr: Yagr, config: YagrConfig): Plugin;
